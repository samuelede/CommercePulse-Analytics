# Installed on top of apache/airflow:2.9.3-python3.11.
# Airflow pins SQLAlchemy 1.4.x, so SQLAlchemy is deliberately NOT pinned here.
# pandas/numpy held to match the analytics layer; numpy < 2 for pandas 2.2.2.
pandas==2.2.2
numpy==1.26.4
requests
python-dotenv
psycopg2-binary